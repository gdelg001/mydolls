<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        android:id="@+id/doll_button"
        android:layout_width="320dp"
        android:layout_height="91dp"
        android:layout_marginTop="20dp"
        android:fontFamily="cursive"
        android:gravity="left"
        android:textColor="@color/purple"
        android:textSize="55sp"
        android:textStyle="bold"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:padding="4dp"
        android:id="@+id/doll_image"
        android:scaleType="centerInside"
        android:layout_marginEnd="17dp"
        app:layout_constraintEnd_toEndOf="@id/doll_button"
        app:layout_constraintTop_toTopOf="@id/doll_button"
        app:layout_constraintBottom_toBottomOf="@id/doll_button"
        android:contentDescription="@string/ala_image"/>

</androidx.constraintlayout.widget.ConstraintLayout>